# Docker Compose Override for Port Conflicts
# This completely replaces the nginx ports to avoid conflicts

version: '3.8'

services:
  nginx:
    ports:
      - "8081:80"   # Replace 8080 with 8081
      - "8444:443"  # Replace 8443 with 8444
