# Docker Compose Override for Port Conflicts
# Use this when standard ports 8080/8443 are already in use

version: '3.8'

services:
  nginx:
    ports:
      - "8081:80"   # Use 8081 instead of 8080
      - "8444:443"  # Use 8444 instead of 8443
    
  # All other services remain the same
